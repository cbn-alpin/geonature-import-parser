USER CSV file report
-------------------------------------------------------------------------
List of lines with unknown organism codes:
{% set ns = namespace(total=0) -%}
{% for code, lines in reports['organism_code_unknown_lines'].items() -%}
    {% set ns.total = ns.total + lines | length -%}
    {{ code }}: {{ lines | join(', ') }}
{% endfor %}
    Total: {{ ns.total }}
-------------------------------------------------------------------------
Script time:
    Elapsed: {{ elapsed_time }}
